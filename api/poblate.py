from matplotlib.style import available
from .models import *

@staticmethod
def poblate():
    # Amount.objects.create(id=1, amount='Low')
    # Amount.objects.create(id=2, amount='Meduim')
    # Amount.objects.create(id=3, amount='High')

    # Added.objects.create(id=1, name='queso', available=True)
    # Added.objects.create(id=2, name='jamon', available=True)

    # addeds = Added.objects.all()
    # amounts = Amount.objects.all()

    # AmountAdded.objects.create(id=1, added=addeds[0], amount=amounts[0], price=10)
    # AmountAdded.objects.create(id=2, added=addeds[1], amount=amounts[0], price=15)
    # AmountAdded.objects.create(id=3, added=addeds[0], amount=amounts[1], price=15)
    # AmountAdded.objects.create(id=4, added=addeds[1], amount=amounts[1], price=20)
    # AmountAdded.objects.create(id=5, added=addeds[0], amount=amounts[2], price=20)
    # AmountAdded.objects.create(id=6, added=addeds[1], amount=amounts[2], price=25)

    # BaseOffer.objects.create(id=1, name='Base grande', base_price=120)
    # BaseOffer.objects.create(id=2, name='Base mediana', base_price=90)
    # BaseOffer.objects.create(id=3, name='Base pequeña', base_price=60)
    
    # added_list = AmountAdded.objects.all()
    # bases = BaseOffer.objects.all()

    # Offer.objects.create(id=1, base_offer=bases[0], amount_added=added_list[0])
    # Offer.objects.create(id=2, base_offer=bases[0], amount_added=added_list[1])
    # Offer.objects.create(id=3, base_offer=bases[0], amount_added=added_list[2])
    # Offer.objects.create(id=4, base_offer=bases[0], amount_added=added_list[3])
    # Offer.objects.create(id=5, base_offer=bases[0], amount_added=added_list[4])
    # Offer.objects.create(id=6, base_offer=bases[0], amount_added=added_list[5])
    # Offer.objects.create(id=7, base_offer=bases[1], amount_added=added_list[0])
    # Offer.objects.create(id=8, base_offer=bases[1], amount_added=added_list[1])
    # Offer.objects.create(id=9, base_offer=bases[1], amount_added=added_list[2])
    # Offer.objects.create(id=10, base_offer=bases[1], amount_added=added_list[3])
    # Offer.objects.create(id=11, base_offer=bases[1], amount_added=added_list[4])
    # Offer.objects.create(id=12, base_offer=bases[1], amount_added=added_list[5])
    # Offer.objects.create(id=13, base_offer=bases[2], amount_added=added_list[0])
    # Offer.objects.create(id=14, base_offer=bases[2], amount_added=added_list[1])
    # Offer.objects.create(id=15, base_offer=bases[2], amount_added=added_list[2])
    # Offer.objects.create(id=16, base_offer=bases[2], amount_added=added_list[3])
    # Offer.objects.create(id=17, base_offer=bases[2], amount_added=added_list[4])
    # Offer.objects.create(id=18, base_offer=bases[2], amount_added=added_list[5])
    pass